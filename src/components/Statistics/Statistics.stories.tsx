import { Meta, Story } from '@storybook/react'
import { Statistics } from './Statistics'

export default {
  title: 'Components/Statistics',
  component: Statistics,
} as Meta

export const Default: Story = () => <Statistics />
