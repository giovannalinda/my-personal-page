import { Meta, Story } from '@storybook/react'
import { Skills } from './Skills'

export default {
  title: 'Components/Skills',
  component: Skills,
} as Meta

export const Default: Story = () => <Skills />
