import { Meta, Story } from '@storybook/react'
import { WhatsappButton } from './WhatsappButton'

export default {
  title: 'Components/WhatsappButton',
  component: WhatsappButton,
} as Meta

export const Default: Story = () => <WhatsappButton />
