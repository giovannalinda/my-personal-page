import { Meta, Story } from '@storybook/react'
import { AboutMe } from './AboutMe'

export default {
  title: 'Components/AboutMe',
  component: AboutMe,
} as Meta

export const Default: Story = () => <AboutMe />
