import { Meta, Story } from '@storybook/react'
import { Companies } from './Companies'

export default {
  title: 'Components/Companies',
  component: Companies,
} as Meta

export const Default: Story = () => <Companies />
